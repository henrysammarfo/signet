# Install only server deps; full repo stays available for ../shared imports.
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --prefix server", "npm ci --prefix shared --omit=dev"]

[phases.build]
cmds = ["echo x402-server-ready"]

[start]
cmd = "npm run start --prefix server"
