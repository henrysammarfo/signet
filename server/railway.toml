# Deprecated: use /railway.toml at repo root.
# Railway root directory must be unset (/) so shared/ is in the build context.
# If this path is set in Railway service settings, change it to /railway.toml.
